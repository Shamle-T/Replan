export default function Home() {
  return (
    <main className="foundation-shell">
      <section className="foundation-card" aria-labelledby="replan-title">
        <p className="eyebrow">Constraint-aware planning</p>
        <h1 id="replan-title">Replan</h1>
        <p>The scheduling engine and interactive daily planner are being assembled in focused phases.</p>
      </section>
    </main>
  );
}
